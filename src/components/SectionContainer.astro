---
---
<section class="container">
  <slot />
</section>

<style>
  .container {
    max-width: var(--container-7xl); /* 80rem (1280px) */
    margin-inline: auto;
    padding-inline: calc(var(--spacing) * <7>);

  }
</style>